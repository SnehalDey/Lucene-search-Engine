In order to create a jar file
Go to files and select export

From the menu select java, and from the sub menu select the Runnable jar file

In launch configurations select app-final

for library handling select Package required libraries into generated jar

And then export it to any destination you want to and click finish.